# src/transition_table.py
from typing import Dict, Any
from syntax_tree import Nodo

def construir_tabla_transicion(raiz: Nodo) -> Dict[Any, Any]:
    """
    Construye la tabla de transición del AFD a partir de la función siguiente posición.
    
    Parámetros:
        raiz (Nodo): Raíz del árbol sintáctico.
    
    Retorna:
        Dict: Tabla de transición que mapea estados y símbolos a nuevos estados.
    """
    pass
